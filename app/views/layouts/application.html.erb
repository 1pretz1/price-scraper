<!DOCTYPE html>
<html>
  <head>
    <link rel="icon" type="image/png" href="/assets/favicon.png">
    <meta name="viewport" content="width=device-width, initial-scale=0.7">
    <title>PriceScraper</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class='page-bg'></div>
    <div class="navbar-outer">
      <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="/">Price Monitor</a></li>
        </div>
        <ul class="nav navbar-nav">
          <% if current_user %>
            <li>
            <%= link_to current_user.email, user_path(current_user.id) %>
            </li>
            <li>
            <%= link_to "New URL", new_product_path %>
            </li>
            <% if admin? %>
              <li>
              <%= link_to "Add new website", new_product_website_path %>
              </li>
            <% end %>
          <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if current_user %>
            <li>
            <%= link_to(raw('<span>Log out</span>'), '/logout')  %>
            </li>
          <% else %>
            <li>
            <%= link_to(raw('<span>Sign up</span>'), new_user_path)  %>
            </li>
            <li>
            <%= link_to(raw('<span>Login</span>'), '/login') %>
            </li>
          <% end %>
        </ul>
      </div>
      </nav>
    </div>
    <% flash.each do |key, value| %>
      <div align='center' class="<%= flash_class(key) %>">
        <%= value %>
      </div>
    <% end %>

    <%= yield %>

    <footer>
    <p class='footer_p'>Created by: Peter Hartshorn</p>
    </footer>

  </body>
</html>
